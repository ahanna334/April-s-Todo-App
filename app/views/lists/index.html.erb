<br>
<h1>Lists</h1>


<div id="lists">
<% if @lists.empty? %>
  No records found!
<% else %>
  <% @lists.each do |list| %>
    <%= render list %>
    <p>
      
    </p>
  <% end %>
  <% end %>
</div>

<%= link_to "New list", new_list_path %>
